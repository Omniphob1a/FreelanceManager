apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: projects-db
  namespace: fm-app
spec:
  serviceName: "projects-db"
  replicas: 1
  selector:
    matchLabels:
      app: projects-db
  template:
    metadata:
      labels:
        app: projects-db
    spec:
      containers:
      - name: postgres
        image: postgres:16
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          value: "postgres"
        - name: POSTGRES_PASSWORD
          value: "123"
        - name: POSTGRES_DB
          value: "projects"
        readinessProbe:
          exec:
            command:
            - sh
            - -c
            - "pg_isready -U postgres -d projects -h localhost"
          initialDelaySeconds: 5
          periodSeconds: 10
        volumeMounts:
        - name: pgdata
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: pgdata
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: projects-db
  namespace: fm-app
spec:
  selector:
    app: projects-db
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  type: ClusterIP
